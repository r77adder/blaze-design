import type { Story, StoryDefault } from '@ladle/react';
import { {{pascalCase name}} } from './{{pascalCase name}}';

export default { title: 'Components / {{pascalCase name}}' } as StoryDefault;

export const Default: Story = () => <{{pascalCase name}}>Default</{{pascalCase name}}>;
