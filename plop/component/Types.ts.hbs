import type { HTMLAttributes } from 'react';

// TIP: For variant-driven components, prefer prod's variant naming
// (e.g. variant: 'metadata' | 'largeList') over t-shirt sizes.
// Sizes use the range 'xs' | 'sm' | 'md' | 'lg' | 'xl' | 'xxl'.
// See CONVENTIONS.md for the full policy.
export interface {{pascalCase name}}Props extends HTMLAttributes<HTMLDivElement> {}
