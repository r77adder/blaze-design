// Vetted iOS components — parallel to src/components/
// Promotion gate: an equivalent SwiftUI / native-parity component must exist
// in the iOS app before a component graduates from ios/staging/ to here.
// Today: (empty — first components graduate from ios/staging/ as they mature)
