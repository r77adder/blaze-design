---
name: port-html
description: Convert an HTML mockup file to a real prototype
---

User wants to convert an HTML mockup into a real prototype.

Required argument: `$ARGUMENTS` is the absolute path to the `.html` file. If missing, ask for it.

Follow `.claude/skills/porting-html-to-prototype.md` step by step. Do NOT skip the GAPS.md step at the end — that's how the design system grows.

If the user asks "just do it quickly without the gap analysis," push back: the gap analysis is what makes the prototype real instead of a one-off mockup.
